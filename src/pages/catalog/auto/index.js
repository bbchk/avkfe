import html from './index.html?raw';
import css from './index.scss?inline';

// import { ROUTE_CHANGED_EVENT } from '/config/constants';
// window.addEventListener(ROUTE_CHANGED_EVENT, () => {});

export { html, css };
